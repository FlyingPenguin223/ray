CFLAGS += -O3
CFLAGS += -g3
CFLAGS += -Wall -Wextra

CLINK += -lm
CLINK += -lSDL2

: maps/testmap.tmj |> tiled2c %f %B | clang-format > %o |> src/tiled2cmap.c
: foreach *.c |> gcc $(CFLAGS) -c %f -o %o |> build/%B.o
: build/*.o |> gcc $(CFLAGS) %f -o %o $(CLINK) |> build/main.out
